<!DOCTYPE html>
<html lang="en">
	<head>
		<% include partials/head %>
		
			</head>
			<body class="container">
				<header>
					<% include partials/header %>
				</header>
		
				<main>
					<div class="jumbotron">

					<div>
		 
				 
				<h2>Login</h2>
				<form method="post" action="/login" role="form" data-toggle="validator" class="form-horizontal">
	 
		           <% if(typeof(errorMessage) !== 'undefined') {%>
		              <span><%= errorMessage %></span>
		           <% } %>
	 
	 			  <p>This is an ALPHA system, no critical information should be subitted. No liability for security of data or system will be accepted at this stage.</p>
				  
		           <div class="form-group">

				 	<label for="username" class="col-sm-2 control-label">username</label>
					<div class="col-sm-10">
		            <input type="text" class="form-control" name="username" id="username" required/> <br>
					</div>
				</div>
				<div class="form-group">
				 	<label for="password" class="col-sm-2 control-label">password</label>
					<div class="col-sm-10">
		            <input type="password" class="form-control" name="password" id="password" required/> <br>
					</div>
				</div>
			        <p>
						<div class="form-group">
							<div class="col-sm-offset-2 col-sm-10">
			           <button type="submit" name="submit" id="submit" class="btn btn-default">Sign In</button>
				   </div>
				   </div>
			        </p>
		
		
			     </form>
				</main>
		
				<footer>
					<% include partials/footer %>
				</footer>
			</body>
		</html>